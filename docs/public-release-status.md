# Public release status

Status: PRE-MERGE

The public application snapshot is staged on `hardening/public-release`. Release is blocked until the pull-request production gates pass and `main` protection is configured.
